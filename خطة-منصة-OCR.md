# خطة المشروع: منصة تحويل الكتب العربية المصوّرة إلى Markdown

> Arabic Book OCR — Local-first, مجاني بالكامل، بلا أي API سحابي.

المرجع: ملف المتطلبات `arabicbookocrclaudeprompt.md` (٤١ بندًا). هذه الخطة تلخّصه قرارات تنفيذية.

---

## 1. فحص البيئة (نتيجة أولية)

| العنصر | الحالة | ملاحظة |
|---|---|---|
| Python | ✓ 3.11.15 | متوافق مع PaddleOCR 3.x |
| pip | ✓ 24.0 | — |
| PaddleOCR | متاح على PyPI بإصدار 3.7.0 | يتضمن PP-StructureV3 |
| PaddlePaddle | متاح 3.3.1 (إصدار CPU) | ≈ 500MB+ مع النماذج |
| PyMuPDF | متاح 1.28.2 | ترخيص AGPL-3.0 — مقبول للاستخدام المحلي |
| Tesseract | ✗ غير مثبت | محرك بديل لاحقًا، ليس شرطًا للمرحلة A |

⚠ هذا الفحص جرى في بيئة العمل السحابية. الفحص الحاسم يجب أن يتكرر على جهازك
(نظام التشغيل، RAM، وجود GPU) قبل تثبيت النماذج — سؤال مفتوح أدناه.

## 2. القرارات التقنية

| القرار | الاختيار | السبب |
|---|---|---|
| اللغة والخادم | Python + FastAPI | أبسط Stack، بلا Node/React |
| قراءة PDF | PyMuPDF | محلي، سريع، لا يعدّل الملف الأصلي |
| محرك OCR الأساسي | PaddleOCR / PP-StructureV3 | تحليل تخطيط + ترتيب قراءة، ليس OCR خام |
| محرك بديل | Tesseract (`ara`) | خلف نفس الواجهة `OCREngine` |
| الواجهة | HTML/CSS/JS عربي RTL + polling | لا WebSocket في MVP |
| البيانات الداخلية | `PageResult` + `Block` (dataclasses) | Markdown طبقة عرض أخيرة فقط |
| التشغيل | `python run.py` ← `http://127.0.0.1:8000` | بلا Docker |

## 3. المعمارية

```text
PDF (لا يُلمس)
 ↓ pdf.py            صفحة بصفحة، بلا تحميل كامل في RAM
Page image (data/work/)
 ↓ engines/base.py   OCREngine interface
 ↓ engines/paddleocr_engine.py   layout + OCR + reading order
PageResult { page_number, blocks[{type, bbox, reading_order, text, confidence}] }
 ↓ markdown.py       Renderer محافظ: لا تصحيح، لا حذف تشكيل، لا دمج أسطر الشعر
pages/NNN.md (front matter: source_page, ocr_engine, verified)
 ↓ pipeline.py
book.md + metadata.json (data/output/اسم-الكتاب/)
```

- فشل صفحة ← يُسجَّل `{page, status: "error"}` ويستمر الكتاب؛ لا صفحة فارغة تدّعي النجاح.
- البيانات الوسيطة (`source.png`, `layout.json`, `ocr.json`) تُحفظ في `data/work/` وقابلة للحذف.
- التصميم يسمح لاحقًا بإعادة معالجة الصفحات الفاشلة فقط وبمقارنة المحركات (Benchmark) دون بنائهما الآن.

## 4. مراحل التنفيذ

| المرحلة | الناتج | معيار القبول |
|---|---|---|
| A | نواة: صفحة واحدة ← `PageResult` ← Markdown + اختبار | اختبار يمر على PDF محلي تجريبي |
| B | كتاب كامل ← `pages/*.md` | معالجة تسلسلية بذاكرة ثابتة |
| C | `book.md` + `metadata.json` + `README.md` | بنية الناتج كاملة (§11) |
| D | FastAPI + واجهة RTL بسيطة | اختيار PDF ← بدء التحويل |
| E | حالة التقدم (polling) | صفحة حالية، نسبة، حالة، محرك |

نتوقف بعد كل مرحلة لمراجعتك — خاصة بعد A (§40).

## 5. المخاطر الحقيقية

1. **جودة العربية في PaddleOCR**: نموذج التعرف العربي أضعف نضجًا من الصيني/الإنجليزي.
   المعالجة: المرحلة A تختبر ذلك أولًا على صفحة حقيقية قبل أي بناء إضافي؛ إن خابت الدقة نقيس Tesseract مبكرًا.
2. **ترتيب القراءة RTL**: منطق PP-StructureV3 مضبوط أساسًا لليسار←يمين.
   المعالجة: طبقة `reading_order` خاصة بنا فوق مخرجات التخطيط، لا اعتماد أعمى.
3. **حجم التثبيت**: PaddlePaddle + النماذج ≈ 1GB أول مرة (تنزيل لمرة واحدة، ثم Offline كليًا).
4. **الحواشي والشعر**: نلتزم المحافظة (لا `re.sub(r"\s+")` شامل، لا دمج أسطر)؛ الحواشي قسم منفصل فقط عند ثقة التخطيط.

## 6. حدود MVP (لن نبني)

تسجيل دخول، قواعد بيانات، Cloud، تكاملات، محرر متقدم، Dashboard، تصحيح بالذكاء الاصطناعي، telemetry — §3 و§30 من المتطلبات.

## 7. أسئلة مفتوحة قبل بدء المرحلة A

1. **مكان الكود**: مستودع جديد `arabic-book-ocr` أم مجلد داخل هذا المستودع؟ (الأنظف: مستودع مستقل).
2. **جهازك**: نظام التشغيل وحجم RAM ووجود GPU؟ (يحدد إصدار PaddlePaddle وسرعة المعالجة).
3. **PDF اختبار**: هل توفّر صفحات قليلة من كتاب عربي حقيقي؟ سنبدأ بـfixture مولّد محليًا، لكن الحكم على الدقة يحتاج مسحًا حقيقيًا (§35).
