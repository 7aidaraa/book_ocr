# حالة المشروع — نقطة التسليم

آخر تحديث: 2026-09-03 · الفرع: `main` · الاختبارات: 23/23 ✓

هذا الملف يلخّص كل ما أُنجز وكل ما تعلّمناه تجريبيًا، ليبدأ أي مطوّر (أو محادثة
جديدة) من هنا دون إعادة اكتشاف ما اكتُشف.

---

## 1. ما المشروع

منصة محلية مجانية تحوّل كتب PDF العربية **المصوّرة ضوئيًا** إلى Markdown،
مع الحفاظ على ارتباط كل نص بصفحته الأصلية. الفلسفة الحاكمة: المصدر مقدّس،
OCR لا يعيد كتابة النص ولا يصححه بالتخمين، والدقة أهم من السرعة.

## 2. الحالة الحالية: يعمل ومُجرَّب على كتاب حقيقي

| | |
|---|---|
| كتاب مُنجز | مجمع البحرين — الطريحي، **569 صفحة** |
| النتيجة | صفر أخطاء تقنية، `book.md` ≈ 2 م.ب |
| المحرك | Tesseract 5.5.3 + `ara` |
| مكان الناتج | `C:\Users\7aida\book_ocr-main\book_ocr-main\data\output\book\` |
| مثال ثقة | صفحة 14 = 0.783 |

## 3. القرارات المحسومة تجريبيًا (لا تُعَد تجربتها)

**أ. Tesseract هو المحرك الافتراضي، لا PaddleOCR.**
السبب: PaddleOCR يُلصق الكلمات العربية بلا مسافات. العلة في مكتبة PaddleX نفسها —
دالة `format_line` في `layout_objects.py` تضيف مسافة فقط بعد الحروف اللاتينية
(`is_english_letter`)، فتعامل العربية كالصينية. ليست علة في كودنا.

| المحرك | نفس الجملة (صفحة 14) |
|---|---|
| PaddleOCR | `الحمدلمنخلق الإنسانوعلمه البيان والتبيانوأوضحلهالهدى…` |
| Tesseract | `الحمد لمن خلق الإنسان» وعلمه البيان والتبيان وأوضح له الهدى…` |

**ب. `tessdata_best` لا يستحق الترقية.** اختُبر على 4 صفحات متنوعة: إصلاحاته
(~1–2) يعادلها أخطاء مستحدثة (~3)، والباقي فروق ترقيم. الخطأ المتبقي (مثل
`ححص` وصوابها `خُصّ`) مصدره تشويه الطباعة/المسح لا النموذج.

**ج. `--psm 6` مرفوض.** يبدو منطقيًا (صفحة = كتلة نص) لكنه يدمج العنوان والمتن
والشعر في فقرة واحدة — يخالف قاعدة «لا تحوّل الشعر إلى فقرة واحدة». المعتمد:
`--oem 1 -c preserve_interword_spaces=1` مع `psm` التلقائي (تُحقّق أنه لا يغيّر
تقسيم الفقرات).

**د. سقف الدقة.** لا يوجد OCR مجاني محلي بلا أخطاء على مسح متوسط الجودة. البديل
العملي للمراجعة الشاملة هو ترتيب الصفحات بالثقة (انظر §5).

**هـ. MarkItDown لا يصلح.** يستخرج طبقة النص الموجودة مسبقًا فقط
(`pdfplumber`/`pdfminer`) — على كتاب مصوّر يُخرج ملفًا فارغًا. ليس محرك OCR.

## 4. البنية

```text
app/
├── models.py     PageResult / Block — البيانات المنظمة
├── pdf.py        قراءة صفحة بصفحة (PyMuPDF)، dpi + grayscale
├── markdown.py   Renderer محافظ + page_confidence()
├── pipeline.py   صفحة واحدة: صورة ← OCR ← PageResult ← Markdown
├── book.py       كتاب كامل + استئناف + تقرير المراجعة
├── main.py       FastAPI: رفع، تقدّم، قارئ، ZIP، حذف، فحص ذاتي
├── cli.py        سطر الأوامر
└── engines/      base.py (الواجهة) + tesseract_engine.py + paddleocr_engine.py
```

إضافة محرك ثالث = ملف واحد يرث `OCREngine` فقط؛ لا يُمسّ الخط.

## 5. المراجعة الآلية (بديل قراءة الكتاب كله)

كل صفحة تحمل `ocr_confidence` في ترويستها، وكل كتاب يحصل على **`مراجعة.md`**
يرتّب الصفحات تحت 70% ثقة + الصفحات الفاشلة. عمليًا 5–10% من الصفحات فقط —
المزخرفة والحواشي والرموز (ﷺ، ع). الثقة تقدير المحرك لنفسه لا دقة مقيسة (§23).

## 6. التشغيل

| الحالة | الأداة |
|---|---|
| تحويل يومي | اسحب PDF وأفلته على `convert.bat` |
| واجهة وقارئ | `start.bat` ← `http://127.0.0.1:8000` |
| سطر أوامر | `python -m app.cli "book.pdf"` |
| تجربة Surya | اسحب PDF على `compare-surya.bat` |

`start.bat` و`convert.bat` يكتشفان تغيّر `requirements.txt` ويثبّتان الناقص
تلقائيًا. الاستئناف تلقائي: إعادة التشغيل تتخطى الصفحات الناجحة.

## 7. قيد بيئي مهم

**Cowork** يعمل في آلة لينكس افتراضية تربط مجلدات ويندوز: يقرأ ملفاتك ويكتبها
(النتائج تصل فعلًا إلى `data\output`)، لكنه **لا ينفّذ أوامر ويندوز ولا يثبّت
حزمًا في `.venv`**، وشبكته محدودة (فشل تثبيت PyTorch/Surya عليه).
القاعدة: التثبيتات الثقيلة في **طرفية ويندوز الحقيقية** فقط.

## 8. المهمة المفتوحة الوحيدة: اختبار Surya

المرشّح: <https://github.com/VikParuchuri/surya> — مفتوح المصدر، محلي، تحليل
تخطيط، دعم عربي. لم يُختبر بعد (PyTorch محجوب على شبكة جلسات كلود).

**التنفيذ:** اسحب `C:\book.pdf` على `compare-surya.bat` في طرفية ويندوز.
يثبّت Surya (~2GB) ثم ينزّل نماذجه (~1GB) ثم يقارن الصفحات 14 و50 و150 و300
ويكتب النتائج في `data\work\compare-surya\*.md` (لا يمسّ `data\output`).

**معيار الحكم:** هل يصلح أخطاء الحروف الحقيقية (مثل `ححص` ← `خُصّ`) دون
استحداث أخطاء جديدة؟ وهل يحافظ على المسافات؟ وكم ثانية للصفحة؟
إن تفوّق بوضوح ← يُضاف محركًا ثالثًا. إن لم يتفوّق ← بلغنا السقف، والمكسب
التالي في جودة المسح الضوئي نفسه لا في البرمجيات.

## 9. مسارات بديلة (مبنية وموثّقة)

- **Colab + GPU**: `colab/arabic_book_ocr.ipynb` — للكتب الضخمة بلا حاسوب.
- **Render**: `render.yaml` + `Dockerfile.render` — رابط ثابت، بطيء (0.1 vCPU).
- **Google Drive**: `deploy/google-drive-ocr.gs` — مجلد يعمل وحده (لم يُختبر).

## 10. مراجع

- دليل التشغيل والأعطال الكامل: `دليل-التشغيل.md`
- الخطة الأصلية: `خطة-منصة-OCR.md`
- صفحة الخطوات المنشورة:
  <https://claude.ai/code/artifact/491e81fa-9ded-4dda-b429-e5edaaa0d4b6>
