services:
  - type: web
    name: book-ocr
    runtime: docker
    dockerfilePath: ./Dockerfile.render
    plan: free
    envVars:
      - key: OCR_ENGINE
        value: tesseract
      - key: OCR_DPI
        value: "200"
      - key: PORT
        value: "8000"
