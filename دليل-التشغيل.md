# دليل التشغيل والأعطال

دليل مستقل: كل ما تحتاجه لتشغيل المنصة وحل مشاكلها بنفسك.

---

## المسار الموصى به: حاسوب Windows

الأفضل لكتب 300–500 صفحة: مجاني، بلا حدود جلسات، وبدقة PaddleOCR الكاملة.

### التثبيت (مرة واحدة)

1. ثبّت Python من <https://python.org/downloads> — **فعّل** `Add python.exe to PATH` أثناء التثبيت.
2. نزّل المشروع: <https://github.com/7aidaraa/book_ocr/archive/refs/heads/main.zip> وفك الضغط.
3. انقر نقرًا مزدوجًا على **`start.bat`** — يثبّت كل شيء تلقائيًا (~1GB، عدة دقائق) ثم يفتح المتصفح على `http://127.0.0.1:8000`.

في المرات التالية: نقرة واحدة على `start.bat`.

### الاستخدام

- **من الواجهة:** اختر PDF ← بدء التحويل ← تابع العدّاد ← نزّل ZIP ← احذف من الخادم.
- **من سطر الأوامر** (أسرع للكتب الضخمة):

```bash
python -m app.cli "C:\path\to\book.pdf"
```

الناتج في `data\output\<اسم-الكتاب>\`.

### قياس السرعة قبل الالتزام

حوّل 5 صفحات أولًا واحسب الزمن، ثم اضربه في عدد صفحات الكتاب:

```bash
python -m app.cli "book.pdf" --page 1
```

---

## الأعطال وحلولها

### `'python' is not recognized`
لم تُفعّل `Add python.exe to PATH`. أعد تثبيت Python وفعّلها، أو أعد تشغيل الحاسوب.

### فشلت كل الصفحات — `ConvertPirAttribute2RuntimeAttribute`
خلل oneDNN في PaddlePaddle. مُصلَح في الكود الحالي؛ إن ظهر مجددًا شغّل قبل الأمر:

```bash
set FLAGS_use_mkldnn=0
```

### فشلت كل الصفحات — `No available model hosting platforms`
لا إنترنت أثناء أول تنزيل للنماذج. صِل الإنترنت وأعد المحاولة؛ بعد التنزيل الأول يعمل بلا إنترنت.

### `PP-StructureV3 requires additional dependencies`
ينقص امتداد doc-parser:

```bash
pip install "paddleocr[doc-parser]==3.7.0"
```

### الجهاز بطيء أو الذاكرة تنفد
خفّض الدقة وحوّل لصور رمادية:

```bash
set OCR_DPI=200
set OCR_GRAYSCALE=1
```

### المحرك لا يعمل ولا أعرف السبب
افتح الواجهة واضغط **«فحص سريع لمحرك OCR 🩺»** أسفل الصفحة — يخبرك خلال ثوانٍ بالمحرك والمعالج والخطأ الكامل.
أو من الطرفية: `curl http://127.0.0.1:8000/api/selftest`

### انقطع التحويل في منتصف كتاب كبير
شغّل نفس الأمر ثانيةً — المنصة تتخطى الصفحات الناجحة وتعالج الفاشلة فقط.
لإعادة الكل: أضف `--no-resume`.

### أريد Tesseract بدل PaddleOCR (يحافظ على مسافات الكلمات العربية — انظر §الاختيار بين المحركات)
ثبّت Tesseract مع حزمة العربية من <https://github.com/UB-Mannheim/tesseract/wiki>، ثم:

```bash
python -m app.cli "book.pdf" --engine tesseract
```

### تحسين دقة Tesseract للعربية (خطوة واحدة، فرق حقيقي موثّق)

Tesseract يوزّع ثلاث نسخ من كل نموذج لغة، رسميًا وموثقًا (لا تخمين):

| النسخة | الدقة | السرعة |
|---|---|---|
| `tessdata_fast` | أقل | أسرع |
| `tessdata` (الافتراضية، التي ثبّتناها) | متوسطة | متوسطة |
| **`tessdata_best`** | **الأعلى** | أبطأ (مقبول لكتاب يُحوَّل مرة واحدة) |

للترقية: نزّل `ara.traineddata` من
<https://github.com/tesseract-ocr/tessdata_best/raw/main/ara.traineddata>
واستبدل به الملف الموجود في:
```
C:\Program Files\Tesseract-OCR\tessdata\ara.traineddata
```
(احتفظ بنسخة من الملف القديم قبل الاستبدال إن أردت الرجوع عنه).

⚠ **جُرِّب ورُفِض:** ضبط `--psm 6` (يفترض الصفحة كتلة نص واحدة) يبدو منطقيًا لكنه
يدمج العنوان والمتن والشعر في فقرة واحدة بلا فواصل — يخالف قاعدة المشروع
«لا تحوّل الشعر تلقائيًا إلى فقرة واحدة». لذلك أُبقي `psm` الافتراضي (تلقائي)،
واقتصر التحسين على `oem=1` (نموذج LSTM فقط، وهو ما تتطلبه `tessdata_best` أصلًا)
مع `preserve_interword_spaces=1` — تحققتُ أن كليهما لا يغيّر تقسيم الفقرات.

---

## المسارات البديلة

| المسار | متى تستعمله | ملفه |
|---|---|---|
| **Colab + GPU** | لا يوجد حاسوب، وتريد سرعة عالية | `colab/arabic_book_ocr.ipynb` |
| **Render** | رابط ثابت للكتيبات الصغيرة والقراءة | `render.yaml` |
| **Google Drive** | تريده مجلدًا يعمل وحده بلا تشغيل | `deploy/google-drive-ocr.gs` |

---

## قواعد ثابتة في المشروع

- ملف PDF الأصلي لا يُعدَّل أبدًا.
- لا تصحيح للنص بالذكاء الاصطناعي، ولا حذف للتشكيل، ولا دمج لأسطر الشعر.
- كل صفحة تحتفظ برقم صفحتها الأصلية في `source_page`.
- الصفحة الفاشلة تُعلَّم `status: error` ولا تُنتج ملفًا فارغًا يبدو ناجحًا.
