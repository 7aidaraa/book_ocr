# Core (Phase A)
pymupdf==1.28.2
paddlepaddle==3.3.1
paddleocr[doc-parser]==3.7.0   # doc-parser extras required by PP-StructureV3

# Tests
pytest==9.1.1

# Later phases (D/E) — uncomment when the web UI lands:
# fastapi
# uvicorn
